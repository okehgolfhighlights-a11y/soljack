import React from "react";

export default function Tables() {
  return (
    <div
      style={{
        marginTop: "24px",
        padding: "16px",
        borderRadius: "12px",
        background: "rgba(255,255,255,0.08)",
        border: "1px solid rgba(255,255,255,0.15)",
      }}
    >
      <h3 style={{ marginBottom: "12px" }}>Tables</h3>

      <div
        style={{
          opacity: 0.7,
          fontSize: "14px",
        }}
      >
        No active tables yet.
      </div>
    </div>
  );
}